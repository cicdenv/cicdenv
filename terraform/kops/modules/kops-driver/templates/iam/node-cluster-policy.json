{
  "Version": "2012-10-17",
  "Statement": [
    {
      "Effect": "Allow",
      "Action": [
        "s3:GetBucketLocation",
        "s3:GetEncryptionConfiguration",
        "s3:ListBucket"
      ],
      "Resource": [
        "arn:aws:s3:::${state_store}"
      ]
    },
    {
      "Effect": "Allow",
      "Action": [
        "s3:Get*"
      ],
      "Resource": [
        "arn:aws:s3:::${state_store}/${cluster_name}/addons/*",
        "arn:aws:s3:::${state_store}/${cluster_name}/cluster.spec",
        "arn:aws:s3:::${state_store}/${cluster_name}/config",
        "arn:aws:s3:::${state_store}/${cluster_name}/instancegroup/*",
        "arn:aws:s3:::${state_store}/${cluster_name}/pki/issued/*",
        "arn:aws:s3:::${state_store}/${cluster_name}/pki/private/kube-proxy/*",
        "arn:aws:s3:::${state_store}/${cluster_name}/pki/private/kubelet/*",
        "arn:aws:s3:::${state_store}/${cluster_name}/pki/ssh/*",
        "arn:aws:s3:::${state_store}/${cluster_name}/secrets/dockerconfig"
      ]
    },
    {
      "Effect": "Allow",
      "Action": [
        "kms:*"
      ],
      "Resource": "${state_key_arn}"
    }
  ]
}
