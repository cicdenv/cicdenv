#
# init
#
# terraform/kops/bin/generate-cluster-components.sh {name}
# (cd terraform/kops/clusters/{name}/cluster-config; terraform init -upgrade -backend-config={root_dir}/terraform/backend-config.tfvars)
# ...
